apiVersion: migration.openshift.io/v1alpha1
kind: MigMigration
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: {{ migration_name }}
  namespace: mig
spec:
  stage: false
  migPlanRef:
    name: {{ migration_plan_name }}
    namespace: mig

